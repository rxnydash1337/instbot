# 🚀 Быстрый деплой на VPS

## Один скрипт - все настроится автоматически

```bash
# На сервере выполните:
cd /opt/instabot
chmod +x scripts/*.sh
./scripts/quick-deploy.sh your-domain.com your@email.com
```

## Что делает скрипт:

✅ Устанавливает все зависимости (Docker, Nginx, Certbot)  
✅ Настраивает firewall  
✅ Создает .env файл с автогенерацией токенов  
✅ Настраивает Nginx с проксированием всех сервисов  
✅ Получает SSL сертификат (Let's Encrypt)  
✅ Настраивает автозапуск через systemd  
✅ Создает скрипты для обновления и логов  

## После деплоя:

1. **Заполните .env файл:**
   ```bash
   nano /opt/instabot/.env
   ```

2. **Запустите приложение:**
   ```bash
   cd /opt/instabot
   docker-compose -f docker-compose.prod.yml up -d
   ```

3. **Проверьте работу:**
   - Админ панель: `https://your-domain.com`
   - Лендинг: `https://your-domain.com/landing`

## Полезные команды:

```bash
# Обновление
/opt/instabot/update.sh

# Логи
/opt/instabot/logs.sh

# Перезапуск
systemctl restart instabot
```

## Настройка DNS в reg.ru:

1. Зайдите в панель reg.ru → DNS
2. Добавьте A-записи:
   - `@` → IP вашего сервера
   - `www` → IP вашего сервера
3. Подождите 5-15 минут

Скрипт автоматически проверит применение DNS.

